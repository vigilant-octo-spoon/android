<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.octo_spoon.octo_spoon_mobile">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".MethodologyActivity" />
        <activity android:name=".Book.BookStartActivity" />
        <activity android:name=".Book.StageConnectActivity" />
        <activity android:name=".Book.StageChooseActivity" />
        <activity android:name=".Book.StagePlanActivity" />
        <activity android:name=".Book.StageImplementationActivity" />
        <activity android:name=".Book.StageEvaluateActivity" />
        <activity android:name=".Book.StageCommunicateActivity" />
        <activity android:name=".Book.BookEndActivity"></activity>
    </application>

</manifest>